🟦 STATE_OF_JARVIS.md

(Generated by Astra — 2025-11-18)

📘 1. OVERVIEW

Jarvis currently operates in two runtime modes:

Mode A — Ai-Pi Solo (voice_session.py)

Fully functional

Standalone front-of-house Jarvis

Handles audio, wake word, small queries locally

PC not required

Working exactly as intended

Mode B — Distributed Jarvis (persistent_client.py + PC Brain)

Ai-Pi captures voice

PC Brain performs transcription + reasoning + TTS

Ai-Pi plays audio

Confirmed working in earlier tests (today)

Discovered issues:

time hallucination → fixed

client was not persistent → correct

IC behaviour afterwards became corrupted by Nexus

Mode B is partially complete and requires finishing under Astra’s direction.

📘 2. VERIFIED TRUTHS (as of today)
✔ T1 — Distributed Audio works

Ai-Pi → PC → Ai-Pi roundtrip confirmed.

✔ T2 — Time bug fixed

PC Brain now injects local datetime into GPT context and fast-paths TIME_INTENT.

✔ T3 — Original client was NOT persistent

The test_connection() script behaved exactly as designed:
→ Connect once → send one message → disconnect → exit.

✔ T4 — persistent_client.py is the CORRECT architecture

But not complete.
The design is right; implementation is incomplete and some behaviour drifted.

✔ T5 — GUI (port 5000) must talk to Python REST API (8766)

GUI = visualisation
Python backend = truth
Dual (pull + push) will happen later after v1 is stable.

✔ T6 — GUI ↔ API Integration Complete (2025-11-19)

Express proxy eliminates CORS
Browser → port 5000 → proxy → port 8766
All fetch calls use relative /api/* paths
HUD displays real-time backend state
Event log functional
NO CORS ERRORS

📘 3. CURRENT STATE OF EACH COMPONENT
🟦 Ai-Pi (hardware)

Healthy

Python environment stable

Mode A works

Mode B connects to Brain

GUI bundle installed

REST API on 8766 exists

Node/GUI server on 5000 installed but not integrated

🟦 persistent_client.py (Mode B backend)

Correct components:

LocalQueryHandler

RequestQueue (JSON persistent)

InterfaceAdapter abstraction

REST API (8766)

Brain connection logic

State machine (listening, processing, speaking, stopped)

Incorrect components (need Astra direction):

Attempted transition to text-based behaviour → WRONG

Mode confusion with voice_session.py → WRONG

GUI assumptions → WRONG

Mixed directionality (Python push vs GUI pull) → WRONG

Nexus drift → CRITICAL STOP

This file must be brought back into alignment.

📘 4. GUI (jarvis-ai-console on port 5000)
✔ Built and deployed (Node 20.19.5, React, TypeScript, Vite)
✔ Neural-net HUD functional
✔ Panels show: AI-Pi status, Brain status, Event log
✔ **CONNECTED to backend via Express proxy** (2025-11-19)
✔ REST polling: /api/state and /api/events every 1 second
✔ Buttons present: START JARVIS, STOP JARVIS, TEST TIME, CHECK BRAIN
⏳ Button actions not yet wired to backend handlers

**Architecture:**
- Browser requests /api/* (relative paths)
- Express (port 5000) proxies to Python API (port 8766)
- CORS eliminated (same-origin via proxy)
- Files modified:
  - ~/jarvis_gui/server/routes.ts (added proxy middleware)
  - ~/jarvis_gui/client/src/pages/jarvis-hud.tsx (relative API paths)

📘 5. PC Brain State (WSL Ubuntu)

Brain server online

Whisper, TTS, GPT path functioning

TIME_INTENT fix deployed

Logs active

No changes required today

Mode B coordination is the next phase.

📘 6. EXECUTIVE DECISION MATRIX
✔ **COMPLETE (2025-11-19):**

GUI online and polling backend successfully
Event log displaying backend events
HUD shows real-time state (loop status, brain connection, wake mode)
CORS issue resolved via Express proxy
Phase 1 (REST polling) architecture proven
CHECK_BRAIN tests actual connectivity and flushes queue
Brain queue exposed in /api/state

✔ NOW (Next immediate tasks - VOICE ROUNDTRIP):

1. **Systemd service for backend** - auto-start on boot with all logic complete
2. **PC Brain server** - Whisper + GPT + TTS models running on WSL
3. **Voice capture** - PTT button integration on Ai-Pi
4. **Voice → Brain → Voice** - Complete roundtrip with queueing
5. **HUD reflects all events** - Visual confirmation of entire pipeline

**CRITICAL UNDERSTANDING:**
- Jarvis is VOICE ONLY - no text queries
- Brain actions = Whisper/GPT/TTS (voice processing)
- Queue holds VOICE queries when brain offline
- GUI buttons (start/stop/test/check) are LOCAL - don't need queue
- RequestQueue already exists and persists to disk

GUI polls Python REST API (8766)

Buttons trigger Python REST actions

Persistent client remains HEADLESS

NO audio integration today

NO doc updates from Nexus

NO text interface

NO architecture drift

Astra commands Nexus exclusively

✔ NEXT (After button actions complete):

Option A: Connect PC Brain WebSocket (verify end-to-end brain queries)
Option B: Add WebSocket push channel (Phase 2 - replace polling with push)
Option C: Daemonize services (systemd for auto-start on boot)
Option D: Connect recorder/playback to persistent_client (audio integration)

**Awaiting Astra's priority directive.**

Add health, Dogzilla, calendar, etc.

📘 7. RISKS IDENTIFIED TODAY
🚨 Nexus drift

Mixed three architecture layers

Attempted to convert Mode B into text mode

Misread phases

Generated incorrect conclusions

Nearly overwrote canonical docs

🚨 Emotional + cognitive overload for Sir

Caused by inconsistent feedback and repeated backward steps.

Astra will now directly control Nexus with strict, narrow prompts only.

📘 8. NEW RULE FOR NEXUS (Enforced by Astra)

“Nexus may not update documentation, refactor architecture, redefine phases, or propose behavioural changes unless Astra explicitly instructs it.”

🟦 STATE_OF_JARVIS.md COMPLETE