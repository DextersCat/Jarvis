# JARVIS Daily TODO — 2025-11-20

- ✅ Phase 0 — Locked Baseline (today, done)
  - Create `JARVIS_STATE_2025-11-20_EXTENDED.md` (golden snapshot).
  - Confirm `jarvis_lite.py`, `server.py`, `jarvis_brain.py`, `brain_hud.py` are consistent.
  - Rule: Architecture changes must reference the state doc before edits.

## 1) Stability & UX (Body + Queue + Start Script)

- ⬜ Pi Start Script (manual; no systemd yet)
  - Research current start command (paths, env, logs).
  - Propose `start_jarvis.sh`: cd into project, (optional) activate env, run `jarvis_lite.py`, logs to `~/jarvis_logs/`.
  - Test from fresh Pi boot: `./start_jarvis.sh`, open HUD, ensure loop online, “Jarvis, what time is it?” works.
  - Verify: document exact command in `JARVIS_STATE_*.md` and pin to desktop/notes.

- ⬜ Queue Management via HUD
  - Research: backend queue file format + existing `clear_queue`; HUD React wiring for actions/state.
  - Propose: extend `/api/state` with `queue_length` (+ optional `queue_items` with id/created_at/preview); add `remove_from_queue` with item_id. HUD: Queue panel list with “Clear queue” + per-item “Remove”.
  - Test: Brain offline → speak 2–3 commands → queue shows in HUD; remove one; clear all.
  - Verify: no crashes; `queue.json` matches HUD; add short note to state doc.

## 2) File Handling with F:\JARVIS_VAULT

- ⬜ Design vault layout
  - Research desired doc types (planning, master decks, notes).
  - Propose minimal: `F:\JARVIS_VAULT\docs\inbox\`, `...\\projects\`, `...\\archive\`.
  - Test: create folders + one sample doc.
  - Verify: JARVISBrain lists them via simple test script.

- ⬜ File service module (PC side)
  - Research existing JARVISBrain helpers for file ops.
  - Propose new `file_service.py` with: `list_docs()`, `read_doc(path)`, `save_doc(path, content)`, `create_doc(template_name, project_name)`.
  - Test: direct Python smoke tests in WSL (no voice).
  - Verify: add basic unit/smoke tests.

- ⬜ Intent wiring in JARVISBrain
  - Research where `process_text_input` routes intents.
  - Propose new intents: “Create a new document for X”, “Open my last Y document”, “Summarise document X.”
  - Test: Brain-only (text input) then voice.
  - Verify: docs land under `F:\JARVIS_VAULT\docs\...` with correct content.

## 3) Calendar & Email Integration (V1.5)

- ⬜ Calendar integration
  - Research: `credentials.json` + token paths from `core.config`.
  - Propose intent: “What’s on my calendar today/tomorrow/this week?” → call `CalendarService.get_events_for_next_24_hours()` (or param).
  - Test: Brain-only calls; watch logs.
  - Verify: Jarvis reads calendar via voice.

- ⬜ Email integration
  - Research Gmail scopes + token path from `core.config`.
  - Propose intents: “Summarise my important emails from the last 4 hours.” / “Any emails marked Action or Meeting today?”
  - Test: use test Gmail label if needed.
  - Verify: Jarvis reads subject/sender/snippet clearly.
